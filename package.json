{
  "name": "svrmap-child",
  "version": "1.0.0",
  "description": "\"create as child theme for twentynineteen wordpress theme\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss scss/style.scss style.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  },
  "author": "Rajah V",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.12.0",
    "nodemon": "^1.19.1"
  }
}
